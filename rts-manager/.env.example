# RTS Manager Configuration
# Copy to .env and adjust as needed

# Server port (default: 9091)
RTS_PORT=9091

# Frontend dev server port for CORS (default: 5173)
RTS_FRONTEND_PORT=5173

# Custom CORS origins (comma-separated, optional)
# If not set, defaults to localhost:RTS_PORT and localhost:RTS_FRONTEND_PORT
# RTS_CORS_ORIGINS=http://localhost:5173,http://localhost:9091

# Home directory for searching Ralph files (default: $HOME)
# HOME=/home/agent

# Projects directory relative to home (default: projects)
RTS_PROJECTS_DIR=projects

# Polling intervals in milliseconds
RTS_TMUX_POLL_MS=2000
RTS_SYSTEM_POLL_MS=5000
